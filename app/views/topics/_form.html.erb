 <%= form_for [topic] do |t| %>
 
    <% if topic.errors.any? %>

     <div class="alert alert-danger">

       <h4><%= pluralize(topic.errors.count, "error") %>.</h4>
       <ul>
         <% topic.errors.full_messages.each do |msg| %>
           <li><%= msg %></li>
         <% end %>
       </ul>
     </div>
   <% end %>

   <%= form_group_tag(topic.errors[:name]) do %>
     <%= t.label :name %>
     <%= t.text_field :name, class: 'form-control', placeholder: "Enter topic name" %>
   <% end %>
   
   <%= form_group_tag(topic.errors[:description]) do %>
     <%= t.label :description %>
     <%= t.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter topic description" %>
   <% end %>
   
    <%= form_group_tag(topic.errors[:public]) do %>
     <%= t.label :public %>
     <%= t.check_box :public, class: 'checkbox' %> Public topic
    <% end %>
   
   <div class="form-group">
     <%= t.submit "Save", class: 'btn btn-success' %>
   </div>
 <% end %>
 
